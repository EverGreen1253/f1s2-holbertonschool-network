#!/usr/bin/env bash
# updates the IP addresses in /etc/hosts for localhost and facebook.com

HOSTS_FILE="hosts.test"

sed -i -e 's/^/#/' $HOSTS_FILE
echo "" | tee -a $HOSTS_FILE > /dev/null
echo "127.0.0.2 localhost" | tee -a $HOSTS_FILE > /dev/null
echo "8.8.8.8 facebook.com" | tee -a $HOSTS_FILE > /dev/null
